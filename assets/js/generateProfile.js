function writeTheFile(userAnswers, userInfo){
    siteStart = `https://github.com/${userAnswers.github}/${userAnswers.repo}`
    console.log(siteStart)
    
    }
    
    siteStart = `https://github.com/${userAnswers.github}/${userAnswers.repo}`